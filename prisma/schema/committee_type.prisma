model CommitteeType {
  id              Int    @id @default(autoincrement()) @db.UnsignedTinyInt
  committeeName   String @map("committee_name")
  committeeNameAr String @map("committee_name_ar")

  // Relations
  committees Committee[]

  @@map("COMMITTEE_TYPE")
}
